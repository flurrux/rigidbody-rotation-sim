{
  "name": "@flurrux/rigidbody-rotation-demo",
  "version": "1.0.0",
  "description": "rigidbody-rotation is best rotation",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:bundle-code": "esbuild src/index.ts --bundle --minify --outfile=docs/index.js",
    "build:copy-html": "copy src\\index.html docs\\index.html",
    "build": "npm run build:bundle-code && npm run build:copy-html"
  },
  "author": "Christian Hoffmann <christianhoffmann@outlook.com>",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.28.0"
  }
}
